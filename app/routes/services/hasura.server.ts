export const Template = ({ fields, token }: any) => {
  //const { resolved, mutation, query } = useGqty({
  //  Authorization: `Bearer ${token}`,
  //});
  //return resolved(async () => {
  //ADD YOUR CODE HERE
  //EXAMPLE MUTATION.INSERT({
  // objects: [{fields: fields}],
  // on_conflict: {
  //    constraint: constraint,
  //    update_columns: [update_columns]
  // }
  //})
  //});
};
